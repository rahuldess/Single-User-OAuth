require 'spec_helper'

describe SingleUserOauth do
  it 'has a version number' do
    expect(SingleUserOauth::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
